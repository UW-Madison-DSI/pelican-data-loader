secrets/
.env
tmp/
tmp.ipynb
__pycache__/
.venv/
# uv.lock is tracked: app/Dockerfile copies it and runs `uv sync --frozen`,
# so an untracked lock file makes the image unbuildable from a fresh clone.
